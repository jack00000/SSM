<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

	<mapper namespace="com.whgc.mapper.UserMapper">
	    <insert id="add" parameterType="User" >
	        insert into User_ ( name ,password) values (#{name},#{password})    
	    </insert>
	    
	    <delete id="delete" parameterType="User" >
	        delete from User_ where id= #{id}   
	    </delete>
	    
	    <select id="get" parameterType="_int" resultType="User">
	        select * from   User_  where id= #{id}    
	    </select>

	    <update id="update" parameterType="User" >
	        update User_ set name=#{name} where id=#{id}    
	    </update>
	    <select id="list" resultType="User">
	        select * from   User_      
	    </select>
	    
	      <select id="getBynp" parameterType="string" resultType="User">
	        select * from   User_  where name=#{0} and password=#{1}   
	    </select>
	         <select id="listByPage" parameterType="_int" resultType="User">
	        select * from   User_  limit #{0} , #{1}  
	    </select>
	    
	    <select id="getTotal"  resultType="int">
	        select count(*) from User_
	    </select>
	     <select id="getIdByName" parameterType="string" resultType="int">
	        select id from User_ where name=#{name}
	    </select>
	</mapper>

